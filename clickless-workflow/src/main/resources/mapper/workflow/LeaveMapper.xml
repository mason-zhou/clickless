<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.clickless.workflow.mapper.LeaveMapper">

    <resultMap id="LeaveAndStatusList" type="com.clickless.workflow.domain.Leave">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="duration" property="duration"/>
        <result column="principal" property="principal"/>
        <result column="contact_phone" property="contactPhone"/>
        <result column="leave_type" property="leaveType"/>
        <result column="title" property="title"/>
        <result column="leave_reason" property="leaveReason"/>
        <result column="start_date" property="startDate"/>
        <result column="end_date" property="endDate"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="process_instance_id" property="processInstanceId"/>
        <result column="status" property="status"/>
    </resultMap>

    <select id="getLeaveAndStatusList" resultMap="LeaveAndStatusList">
        SELECT t1.*, t2.* FROM wkfl_biz_leave t1
        LEFT JOIN wkfl_business_status t2
        ON t1.id = t2.business_key
        WHERE t1.username = #{req.username}
        <if test="req.title != null and req.title != ''">
            AND t1.title LIKE CONCAT('%', #{req.title}, '%')
        </if>
        <if test="req.status != null">
            AND t2.status = #{req.status}
        </if>
        ORDER BY t1.create_time DESC
    </select>

</mapper>